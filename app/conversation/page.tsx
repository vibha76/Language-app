'use client'; import {useState} from 'react'; import AppShell from '../../src/components/AppShell';
export default function Conversation(){const [messages,setMessages]=useState([['tutor','ನಮಸ್ಕಾರ! ಹೇಗಿದ್ದೀರಿ?','Hello! How are you?']]);const [input,setInput]=useState('');const send=()=>{if(!input.trim())return;setMessages([...messages,['you',input,''],['tutor','ಚೆನ್ನಾಗಿದ್ದೇನೆ! ನೀವು ಎಲ್ಲಿಂದ ಬಂದಿದ್ದೀರಿ?','I’m fine! Where are you from?']]);setInput('')};return <AppShell><div className="conversation"><div className="conversation-head"><div><span className="kicker">AI CONVERSATION · BEGINNER</span><h1>A real conversation, not a chatbot.</h1><p>Your tutor adapts vocabulary and grammar to your current level.</p></div><div className="tutor-face">✨</div></div><div className="chat-window">{messages.map((m,i)=><div className={`bubble-row ${m[0]}`} key={i}><div className="bubble">{m[1]}{m[2]&&<small>{m[2]}</small>}</div></div>)}</div><div className="chat-input"><button>🎤</button><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Type in Kannada…"/><button onClick={send}>↑</button></div><div className="coach-note">💡 <b>Coach:</b> Try answering with “ನಾನು ಬೆಂಗಳೂರಿನಿಂದ ಬಂದಿದ್ದೇನೆ” — I’ll evaluate grammar after you respond.</div></div></AppShell>}
