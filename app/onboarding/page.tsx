'use client';
import { useState } from 'react';
import Link from 'next/link';
const steps=[['language','What language do you want to learn?',['Kannada · ಕನ್ನಡ','Sanskrit · संस्कृतम्','Hindi · हिन्दी','French · Français']],['why','Why are you learning?',['School','Career','Travel','Heritage','Exams','Personal interest']],['level','Where are you starting?',['Complete beginner','A few words','Basic','Intermediate','Advanced']],['time','How much time can you learn?',['5 minutes','10 minutes','15 minutes','30 minutes','60+ minutes']],['skills','What do you want to become great at?',['Speaking','Writing','Reading','Grammar','Pronunciation','Listening','Vocabulary','Exams']]] as const;
export default function Onboarding(){const [i,setI]=useState(0);const [choice,setChoice]=useState('');const s=steps[i];return <main className="onboard"><div className="onboard-top"><Link href="/">Lingua<span>Verse</span></Link><span>{i+1} / {steps.length}</span></div><div className="progress-line"><i style={{width:`${((i+1)/steps.length)*100}%`}}/></div><section><span className="kicker">PERSONALIZE YOUR JOURNEY</span><h1>{s[1]}</h1><p>We’ll use this to tune your lessons, review schedule and AI tutor.</p><div className="choices">{s[2].map(x=><button className={choice===x?'chosen':''} onClick={()=>setChoice(x)} key={x}>{x}<span>{choice===x?'✓':'+'}</span></button>)}</div><button className="primary next" disabled={!choice} onClick={()=>{setChoice('');setI(Math.min(i+1,steps.length-1))}}>{i===steps.length-1?'Build my path':'Continue'} →</button></section></main>}
